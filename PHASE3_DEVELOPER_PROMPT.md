# PHASE 3 — ПОЛНАЯ ПЕРЕДЕЛКА HL SIMULATOR ПОД ТОЧНУЮ КОПИЮ HYPERLIQUID

## КОНТЕКСТ
Ты работаешь над HL Simulator — paper trading симулятором Hyperliquid. Проект: Next.js 14 + Tailwind + Supabase + lightweight-charts. Текущий деплой: https://hl-simulator.vercel.app

Я провёл полный визуальный аудит, открыв в Chrome:
- Реальный Hyperliquid: https://app.hyperliquid.xyz/trade, /portfolio, /earn
- Наш симулятор: https://hl-simulator.vercel.app/trade, /portfolio, /faucet

ЗАДАЧА: Сделать наш симулятор ВИЗУАЛЬНО ИДЕНТИЧНЫМ реальному Hyperliquid. Те же страницы, те же кнопки, тот же layout, те же цвета, до мелочей. Только 3 страницы рабочие (Trade, Portfolio, Earn), остальные — в навигации есть но disabled (не кликабельны). Наш Faucet переезжает внутрь Earn страницы.

---

## КРИТИЧЕСКИЙ BUGFIX (СДЕЛАТЬ ПЕРВЫМ!)

### BUG: Portfolio и Faucet страницы зависают на бесконечном спиннере

**Проблема:** После логина через Trade страницу, при переходе на /portfolio или /faucet — бесконечный loading spinner. Trade page работает нормально.

**Вероятная причина в `src/hooks/useAuth.ts`:**
- useEffect имеет зависимости `[supabase, fetchCallback]` которые могут пересоздаваться на каждом рендере
- `getSession()` может не резолвиться если supabase client пересоздаётся
- `fetchAccount` в dependency array вызывает бесконечный loop

**Как починить:**
1. В `useAuth.ts` — обернуть `fetchAccount` в `useCallback` с правильными dependencies
2. Убедиться что `supabase` client стабилен (создаётся один раз)
3. Добавить try/catch + finally с `setLoading(false)` чтобы loading ВСЕГДА становился false
4. Добавить таймаут — если loading > 5 секунд, принудительно ставить false

**Пример фикса:**
```typescript
useEffect(() => {
  let mounted = true;

  const init = async () => {
    try {
      const { data: { session } } = await supabase.auth.getSession();
      if (!mounted) return;
      setUser(session?.user ?? null);
      if (session?.user) {
        await fetchAccount(session.user.id);
      }
    } catch (e) {
      console.error("Auth init error:", e);
    } finally {
      if (mounted) setLoading(false);
    }
  };

  init();

  // Safety timeout
  const timeout = setTimeout(() => {
    if (mounted) setLoading(false);
  }, 5000);

  return () => { mounted = false; clearTimeout(timeout); };
}, []); // Минимальные dependencies!
```

**Проверить:** После фикса /portfolio и /faucet должны загружаться без зависания.

---

## PHASE 3.1 — НАВИГАЦИЯ (Navigation.tsx — полная переделка)

### Текущее состояние:
- Высота: 48px
- 3 ссылки: Trade, Portfolio, Faucet
- Справа: Live/REST/Sim статус + Equity + Sign Out
- Лого: прямоугольный SVG rect

### Как должно быть (точная копия Hyperliquid):

**Верхний header bar (48px):**
```
[Logo: два тёмно-бирюзовых кружка] [Hyperliquid (italic)] | Trade | Portfolio | Earn | Vaults | Staking | Referrals | Leaderboard | More ▽ |  ... [Connect кнопка / Equity] [🌐] [⚙️]
```

**Реализация:**

1. **Лого** — заменить `<rect>` на два кружка:
```jsx
<svg width="24" height="24" viewBox="0 0 24 24">
  <circle cx="8" cy="12" r="6" fill="#00d8c4" />
  <circle cx="16" cy="12" r="6" fill="#00d8c4" />
</svg>
```
Текст "Hyperliquid" — добавить `italic` стиль (`font-style: italic` или `className="italic"`)

2. **Навигационные ссылки (ВСЕ 8 пунктов):**
```typescript
const NAV_ITEMS = [
  { label: "Trade", href: "/trade", enabled: true },
  { label: "Portfolio", href: "/portfolio", enabled: true },
  { label: "Earn", href: "/earn", enabled: true },
  { label: "Vaults", href: "#", enabled: false },
  { label: "Staking", href: "#", enabled: false },
  { label: "Referrals", href: "#", enabled: false },
  { label: "Leaderboard", href: "#", enabled: false },
  { label: "More", href: "#", enabled: false, hasDropdown: true },
];
```

3. **Disabled ссылки:** `text-t4 cursor-not-allowed pointer-events-none opacity-50` — НЕ кликабельны

4. **Active ссылка:** `text-acc border-b-2 border-acc` (как сейчас, но для всех 8)

5. **Правая часть (ml-auto):**
   - Когда НЕ залогинен: кнопка **"Connect"** — `bg-acc text-black px-4 py-1.5 rounded-full text-[13px] font-medium` (скруглённая pill-кнопка, точно как у HL)
   - Когда залогинен: показывать **Equity** badge + **Sign Out** кнопку (как сейчас)
   - Иконка глобуса 🌐 (20x20, text-t2, декоративная)
   - Иконка шестерёнки ⚙️ (20x20, text-t2, декоративная)
   - Использовать lucide-react иконки: `Globe`, `Settings`

6. **"SIM" badge** — ОСТАВИТЬ как есть (наша фича), расположить рядом с "Hyperliquid"

7. **Mobile bottom nav** — обновить: показывать только 3 рабочие ссылки (Trade, Portfolio, Earn)

### Новый компонент: Footer.tsx

Создать footer внизу каждой страницы:
```
┌─────────────────────────────────────────────────────────┐
│ ● Online        Docs   Support   Terms   Privacy Policy │
└─────────────────────────────────────────────────────────┘
```

**CSS:** `fixed bottom-0 left-0 right-0 h-[28px] flex items-center justify-between px-4 bg-bg border-t border-brd text-[11px] text-t3 z-40`
- Левая часть: зелёный кружок (6x6 bg-green-500 rounded-full) + "Online"
- Правая часть: 4 ссылки (text-t3 hover:text-t1, пока некликабельные)
- На мобиле: скрыть footer (`hidden md:flex`) потому что там bottom nav

**Файлы:** `src/components/Navigation.tsx`, новый `src/components/Footer.tsx`, все page.tsx файлы (добавить Footer)

---

## PHASE 3.2 — TRADE PAGE: 3-КОЛОНОЧНЫЙ LAYOUT (trade/page.tsx)

### Текущее состояние:
```css
md:grid-cols-[1fr_280px] md:grid-rows-[1fr_200px]
```
2 колонки: Chart | OrderForm+OrderBook

### Как должно быть (точная копия HL):
```
┌──────────────────────┬──────────┬─────────────┐
│                      │ Order    │ Market │ Limit│
│   TradingView Chart  │ Book     │   Pro ▽     │
│                      │ (280px)  │ [Buy] [Sell] │
│                      │          │ OrderForm   │
│                      │          │ (300px)     │
├──────────────────────┤          ├─────────────┤
│ Bottom Panel         │          │ Account     │
│ (200px)              │          │ Equity      │
├──────────────────────┴──────────┴─────────────┤
│ Footer                                        │
└───────────────────────────────────────────────┘
```

**Новый grid:**
```css
md:grid md:grid-cols-[1fr_280px_300px] md:grid-rows-[1fr_200px]
```

**Расположение компонентов:**
1. **Chart:** `md:row-span-1 md:col-start-1 md:col-span-1` — занимает левую колонку, верхний ряд
2. **OrderBook (средняя колонка):** `md:row-span-2 md:col-start-2` — на всю высоту, 280px
3. **OrderForm + AccountEquity (правая колонка):** `md:row-span-2 md:col-start-3` — на всю высоту, 300px
4. **BottomPanel:** `md:row-start-2 md:col-start-1 md:col-span-1` — под чартом, только 1 колонка

**Borders между колонками:**
- OrderBook: `border-l border-r border-brd`
- OrderForm: `border-l border-brd`
- BottomPanel: `border-t border-brd`

**Мобильный layout (оставить stacked):**
```
[CoinInfoBar]
[Chart - 40vh]
[OrderForm - scrollable]
[OrderBook - 300px]
[BottomPanel - 200px]
[Mobile nav spacer]
```

---

## PHASE 3.3 — COIN INFO BAR (CoinInfoBar.tsx — переделка)

### Текущее:
HYPE/USDC + кнопки монет + Price + 24H Change + High + Low + Funding

### Как должно быть (точная копия HL):
```
⭐ | [●●] HYPE/USDC ▽ | Price: 33,166 | 24H Change: -2,668 / -7.49% | 24H Volume: 254M USDC | Market Cap: 9.9B USDC | Contract: 0x0d01...11ec
```

**Изменения:**

1. **Добавить звёздочку-избранное** (слева, первый элемент):
   - Иконка `Star` из lucide-react (16x16)
   - `text-t4 hover:text-yellow-500 cursor-pointer`
   - При клике меняет цвет (useState, localStorage для сохранения)

2. **Coin selector** — переделать:
   - Убрать отдельные кнопки HYPE/BTC/ETH/SOL
   - Сделать dropdown как у HL: `[coin_icon] HYPE/USDC ▽`
   - При клике — выпадающий список монет
   - Каждая монета: иконка + название + последняя цена

3. **Заменить поля:**
   - УБРАТЬ: 24H High, 24H Low, Funding Rate
   - ДОБАВИТЬ: **24H Volume** (можно захардкодить или получить через API)
   - ДОБАВИТЬ: **Market Cap** (можно захардкодить)
   - ДОБАВИТЬ: **Contract** адрес (укороченный: `0x0d01...11ec`, text-t3, с иконкой копирования)

4. **24H Change** — показывать ДВА значения:
   - Абсолютное изменение: `-2,668`
   - Процент: `-7.49%`
   - Красный если минус, зелёный если плюс

5. **Price** — оставить крупным (text-[20px] font-bold), добавить формат с запятыми

**Файлы:** `src/components/CoinInfoBar.tsx`, новый `src/components/CoinSelector.tsx`

---

## PHASE 3.4 — ORDER FORM (OrderForm.tsx — переделка)

### Текущее:
2 таба (Market/Limit) + Leverage секция + Size + Slider + Buy button

### Как должно быть (точная копия HL):

**Структура сверху вниз:**

1. **3 таба:** `Market` | `Limit` | `Pro ▽`
   - Pro — disabled, серый, с dropdown иконкой
   - Активный таб: `border-b-2 border-t1 text-t1`
   - Неактивный: `text-t3`

2. **Buy / Sell toggle:**
   - Две кнопки на всю ширину (50/50)
   - Buy активен: `bg-acc text-black font-bold rounded-l`
   - Sell активен: `bg-red text-white font-bold rounded-r`
   - Неактивная кнопка: `bg-s3 text-t3`

3. **Available to Trade:** `text-acc text-[12px]` слева, `X.XX USDC` справа

4. **Price input (показывать ВСЕГДА для Limit, скрывать для Market):**
   - Label: "Price (USDC)"
   - Input field: `bg-s2 border border-brd rounded px-3 py-2`
   - Справа внутри input: кнопка **"Mid"** — `text-acc text-[11px] cursor-pointer`
   - При клике Mid — заполняет текущей mid ценой

5. **Size input:**
   - Input field с placeholder "0.00"
   - Справа: dropdown **"HYPE ▽"** (переключает единицы: HYPE / USDC)
   - Выше input: `Size` label слева

6. **Slider с точками:**
   - Range input 0-100%
   - Визуальные точки на позициях: 0%, 25%, 50%, 75%, 100% (7 точек)
   - Справа от слайдера: input с `%` suffix
   - Стиль точек: `w-[6px] h-[6px] rounded-full bg-s4` (серые точки на трэке)

7. **TIF (Time in Force):**
   - Справа: `TIF` label + **"GTC ▽"** dropdown
   - Опции: GTC, IOC, ALO (все декоративные, только GTC работает)
   - `text-[12px] text-t2`

8. **Submit кнопка (во всю ширину):**
   - Когда залогинен: **"Buy Market"** / **"Sell Limit"** etc
   - Когда НЕ залогинен: **"Connect"** кнопка (`bg-acc text-black`)
   - Buy: `bg-acc text-black py-3 rounded font-bold text-[14px]`
   - Sell: `bg-red text-white py-3 rounded font-bold text-[14px]`

9. **Order details (под кнопкой):**
   - `Order Value` → `N/A` (или $XXX)
   - `Fees` → `0.0700% / 0.0400%`
   - НЕ ПОКАЗЫВАТЬ: Slippage, Est. Liq. Price (убрать из основной формы)

10. **LEVERAGE — перенести:**
    - Убрать секцию leverage из основной формы
    - Добавить маленькую кнопку/badge **"10x"** рядом с Size — при клике открывает попап с настройкой leverage
    - Или: разместить leverage контролы В ОТДЕЛЬНОЙ СЕКЦИИ ниже формы (accordion)

**Файлы:** `src/components/OrderForm.tsx`

---

## PHASE 3.5 — ORDER BOOK: СРЕДНЯЯ КОЛОНКА (OrderBook.tsx + OrderBookTabs.tsx)

### Текущее:
OrderBook + Trades как вкладки под OrderForm в правой колонке (280px)

### Как должно быть:
OrderBook — отдельная СРЕДНЯЯ колонка на всю высоту экрана (280px)

**Структура средней колонки:**

```
┌──────────────────────────┐
│ [Order Book] [Trades]  ⋮ │  ← tabs + menu icon
├──────────────────────────┤
│ 0,001 ▽         HYPE ▽  │  ← precision + unit selectors
├──────────────────────────┤
│ Price   Size(HYPE) Total │  ← headers
├──────────────────────────┤
│ 33.057    45.91  3,819   │  ← asks (red, снизу вверх)
│ 33.054   274.78  3,773   │
│ 33.053    61.45  3,499   │
│ ...                      │
├──────────────────────────┤
│ Spread   0.014   0.042%  │  ← spread row
├──────────────────────────┤
│ 33.039  1,377   2,252    │  ← bids (green, сверху вниз)
│ 33.038   201.93   874    │
│ ...                      │
└──────────────────────────┘
```

**Новые элементы:**

1. **Tabs header:**
   - `Order Book` | `Trades` — как сейчас
   - Добавить: иконка **⋮** (three dots menu) справа — декоративная

2. **Precision selector (НОВЫЙ):**
   - Dropdown: `0.001 ▽`
   - Опции: 0.001, 0.01, 0.1, 1, 10 (влияет на группировку уровней)
   - `text-[11px] bg-s2 border border-brd rounded px-2 py-0.5`

3. **Unit selector (НОВЫЙ):**
   - Dropdown: `HYPE ▽`
   - Опции: HYPE, USDC (меняет единицы Size и Total)
   - Расположение: справа от precision

4. **Колонки таблицы:**
   - `Price` (left-aligned)
   - `Size (HYPE)` (right-aligned)
   - `Total (HYPE)` (right-aligned)

5. **Depth bars:** Оставить как есть (`bg-red/[0.06]` asks, `bg-grn/[0.06]` bids)

6. **Spread row:**
   - `Spread` label + значение + процент
   - `bg-s2 border-y border-brd py-1.5`
   - Центрированный текст

**Файлы:** `src/components/OrderBook.tsx`, `src/components/OrderBookTabs.tsx`, `src/app/trade/page.tsx`

---

## PHASE 3.6 — ACCOUNT EQUITY PANEL (Новый компонент!)

### Текущее: Не существует

### Создать `src/components/AccountEquity.tsx`:

Расположение: правая колонка trade page, ПОД OrderForm (scroll если не помещается)

**Структура:**
```
┌────────────────────────┐
│ Account Equity         │
│ ├─ Spot    $0.00       │
│ └─ Perps   $196,337.33 │
├────────────────────────┤
│ Perps Overview         │
│ ├─ Balance    $X.XX    │
│ ├─ Unrlzd PNL $X.XX   │
│ ├─ Cross Margin  X%    │
│ ├─ Maint. Margin $X    │
│ └─ Cross Leverage Xx   │
├────────────────────────┤
│ [Deposit][Perps↔Spot][Withdraw] │
└────────────────────────┘
```

**CSS:**
```
bg-s1 border-t border-brd p-3 text-[12px]
```

**Данные:**
- Spot: всегда $0.00 (мы не поддерживаем spot)
- Perps: = totalEquity из account.balance + unrealizedPnL
- Balance: = account.balance
- Unrealized PNL: = сумма PnL всех открытых позиций
- Cross Margin Ratio: = usedMargin / totalEquity * 100
- Maintenance Margin: = usedMargin * 0.05 (5% от margin)
- Cross Account Leverage: = totalPositionValue / totalEquity

**Кнопки (3 штуки, в ряд):**
- "Deposit" → ведёт на /earn (наш faucet)
- "Perps ⇄ Spot" → декоративная (disabled)
- "Withdraw" → декоративная (disabled)
- Стиль: `flex-1 py-1.5 text-[11px] border border-brd rounded text-t2 hover:bg-s2`

**Файлы:** новый `src/components/AccountEquity.tsx`, `src/app/trade/page.tsx`

---

## PHASE 3.7 — BOTTOM PANEL (BottomTabsPanel.tsx — расширить)

### Текущие табы: Balances | Positions (2) | Open Orders (3) | Trade History

### Как должно быть:

**Табы (9 штук):**
1. **Balances** — оставить, но добавить колонки: `USDC Value ▽`, `PNL (ROE %)`, `Contract`
2. **Positions** — оставить как есть
3. **Open Orders** — оставить как есть
4. **TWAP** — НОВЫЙ таб (заглушка: "No TWAP orders")
5. **Trade History** — оставить как есть
6. **Funding History** — НОВЫЙ таб (заглушка: "No funding history")
7. **Order History** — НОВЫЙ таб (показывать ВСЕ ордера из order_history, не только pending)
8. **Interest** — НОВЫЙ таб (заглушка: "No interest history") — на PORTFOLIO page
9. **Deposits and Withdrawals** — НОВЫЙ таб (заглушка: "No deposits") — на PORTFOLIO page

**Дополнительные элементы (справа от табов):**

- **Filter ▽** dropdown — декоративный, `text-[12px] text-t2 border border-brd rounded px-2 py-1`
- **Hide Small Balances** checkbox — `flex items-center gap-1.5 text-[12px] text-t2`
  - Checkbox: `w-3.5 h-3.5 rounded border border-brd bg-s3 accent-acc`

**Стиль табов:**
- Активный: `text-t1 border-b-2 border-acc`
- Неактивный: `text-t3 hover:text-t2`
- Размер: `text-[12px] px-3 py-2`
- Пустые/заглушки: серый текст (text-t4)

**Файлы:** `src/components/BottomTabsPanel.tsx`

---

## PHASE 3.8 — PORTFOLIO PAGE (portfolio/page.tsx — полная переделка)

### Текущее: 4 карточки + Margin Overview + 2 таблицы (И ВСЁ СЛОМАНО — спиннер)

### Как должно быть (точная копия HL):

**Layout:**
```
[Navigation]
[Content area - max-width, centered]
  [Title: "Portfolio"]
  [Action buttons row]
  [3 stat cards row]
  [Fees card - left aligned]
  [Bottom tabs panel]
[Footer]
```

**1. Title:** `text-[28px] font-bold text-t1 mb-4` → "Portfolio"

**2. Action buttons (горизонтальный ряд, прокручиваемый):**
```
[Link Staking] [Swap Stablecoins] [Perps ⇄ Spot] [EVM ⇄ Core] [Portfolio Margin] [Send] [Withdraw] [Deposit]
```
- Все кнопки: `border border-brd rounded-full px-4 py-1.5 text-[13px] text-t2 hover:bg-s2`
- "Deposit": `bg-acc text-black` (акцентная, ведёт на /earn)
- Все остальные: декоративные (disabled или показывать tooltip "Coming soon")

**3. Три карточки в ряд (`grid grid-cols-1 md:grid-cols-3 gap-4`):**

**Карточка 1: 14 Day Volume**
```
┌─────────────────────┐
│ 14 Day Volume       │
│ $XXX,XXX            │  ← крупный текст
│ View Volume (link)  │  ← text-acc, text-[12px]
└─────────────────────┘
```
- `bg-s2 border border-brd rounded-lg p-4`
- Volume = сумма всех trade_history за 14 дней

**Карточка 2: PNL Stats**
```
┌────────────────────────────┐
│ Perps + Spot + Vaults ▽  30D ▽ │
│ PNL              $X.XX     │
│ Volume           $X.XX     │
│ Max Drawdown     X.XX%     │
│ Total Equity     $X.XX     │
│ Perps Account    $X.XX     │
│ Spot Account     $0.00     │
│ Earn Balance     $0.00     │
└────────────────────────────┘
```
- Два dropdown в header: period selector (Perps+Spot+Vaults) + time (30D)
- Каждая строка: `flex justify-between py-1 text-[13px]`
- Labels: `text-t3`, Values: `text-t1`

**Карточка 3: Account Value / PNL Chart**
```
┌──────────────────────────┐
│ [Account Value] [PNL]    │  ← 2 таба
│ ┌──────────────────────┐ │
│ │   Line Chart         │ │
│ │   (equity over time) │ │
│ └──────────────────────┘ │
└──────────────────────────┘
```
- Использовать lightweight-charts LineChart
- X axis: даты, Y axis: $
- Данные: можно генерировать из trade_history

**4. Fees Card (под карточками, слева):**
```
┌─────────────────────────┐
│ Fees (Taker/Maker)  Perps ▽ │
│ 0.0450% / 0.0150%       │  ← крупный текст
│ View Fee Schedule (link) │
└─────────────────────────┘
```
- `bg-s2 border border-brd rounded-lg p-4 max-w-xs`

**5. Bottom Tabs Panel:**
- Использовать тот же `BottomTabsPanel` компонент что на trade page
- Но с дополнительными табами: **Interest**, **Deposits and Withdrawals**
- Показывать данные Balances/Positions/etc для текущего аккаунта

**Файлы:** `src/app/portfolio/page.tsx`

---

## PHASE 3.9 — EARN PAGE (Новая страница, заменяет /faucet)

### Текущее: /faucet — карточка с кнопками сумм $1K/$5K/$10K/$50K (СЛОМАНА — спиннер)

### Как должно быть:

**Роутинг:**
- Создать `/earn/page.tsx`
- `/faucet` — редирект на `/earn` (или оставить как alias)
- В Navigation: ссылка "Earn" ведёт на `/earn`

**Layout (точная копия HL Earn):**
```
[Navigation]
[Content area]
  [Title: "Earn"]                    [Supply button - top right]
  [3 stat cards row]
  [Supply table]
  [--- наш Faucet секция ---]
[Footer]
```

**1. Title + Supply button:**
- "Earn" — `text-[28px] font-bold text-t1`
- Кнопка "Supply" справа: `bg-acc text-black px-4 py-1.5 rounded text-[13px] font-medium`

**2. Три карточки (`grid grid-cols-1 md:grid-cols-3 gap-4`):**

**Карточка 1: Your Health Factor**
```
┌─────────────────────┐
│ Your Health Factor  │  ← text-t3, underline dotted
│ --                  │  ← text-[24px] font-bold
└─────────────────────┘
```

**Карточка 2: Your Total Supplied**
```
┌─────────────────────┐
│ Your Total Supplied │
│ $0.00               │
└─────────────────────┘
```

**Карточка 3: Your Total Borrowed**
```
┌─────────────────────┐
│ Your Total Borrowed │
│ $0.00               │
└─────────────────────┘
```

- Все карточки: `bg-s2 border border-brd rounded-lg p-5`

**3. Supply Table:**
```
┌────────┬─────┬───────┬─────────────┬───────────────┬──────────────────┬───────────────────┬──────────────┐
│ Asset↑ │ LTV │  APY  │ Oracle Price│ Your Supplied │ Your Int. Earned │ Total Supplied    │ Action       │
├────────┼─────┼───────┼─────────────┼───────────────┼──────────────────┼───────────────────┼──────────────┤
│ USDC   │ N/A │ 0.14% │ $1.00       │ 0.00 USDC     │ 0.00 USDC        │ 3,023,477 USDC    │ Supply Withdr│
│ USDH   │ N/A │ 0.09% │ $1.00       │ 0.00 USDH     │ 0.00 USDH        │ 2,078,094 USDH    │ Supply Withdr│
└────────┴─────┴───────┴─────────────┴───────────────┴──────────────────┴───────────────────┴──────────────┘
```

- Section header: "Supply" — `text-[16px] font-bold mb-2`
- Table: `w-full text-[13px]`
- Header row: `text-t3 border-b border-brd py-2`
- Data rows: `py-3 border-b border-brd/50`
- APY column: `text-grn` (зелёный)
- Action кнопки: `text-acc text-[12px] cursor-pointer` для "Supply", `text-t3` для "Withdraw"
- Данные: ЗАХАРДКОДИТЬ (USDC + USDH rows, Total Supplied — константы)

**4. НАШ FAUCET — интегрировать внизу:**

Под Supply table добавить секцию "Demo Funds" (наш текущий faucet):
```
┌──────────────────────────────────────────────────────┐
│ 🎮 Demo Funds                                       │
│                                                       │
│ Current Balance: $196,337.33 USDC                    │
│                                                       │
│ [$1,000] [$5,000] [$10,000] [$50,000]               │
│                                                       │
│ [Request Funds]              [Reset to $10,000]      │
│                                                       │
│ Recent Requests:                                      │
│  +$10,000 USDC — 2 min ago                           │
└──────────────────────────────────────────────────────┘
```

- `bg-s2 border border-brd rounded-lg p-5 mt-6`
- Перенести ВЕСЬ функционал из текущего `faucet/page.tsx`

**Файлы:** новый `src/app/earn/page.tsx`, удалить или редирект `src/app/faucet/page.tsx`

---

## PHASE 3.10 — МЕЛКИЕ ДЕТАЛИ ДЛЯ 100% СООТВЕТСТВИЯ

### 1. Шрифт
- Проверить что Inter загружается правильно (Google Fonts)
- Убедиться `font-variant-numeric: tabular-nums` на всех ценах

### 2. Цвета (проверить соответствие):
- Main bg: `#0a0f14` ✓
- Panel bg: `#0f1419` ✓
- Accent (Buy/Connect): `#00d8c4` ← это БИРЮЗОВЫЙ, НЕ зелёный! ✓
- Green (profit): `#00c076` ✓
- Red (sell/loss): `#ff4976` ✓
- Border: `#1a1f2e` ✓

### 3. Размеры текста:
- Навигация: 13px
- Цена в CoinInfoBar: 20px bold
- Order book rows: 11px
- Bottom panel: 11-12px
- Заголовки страниц: 28px bold
- Кнопки: 13-14px

### 4. Page title в browser tab:
- Trade: `"{price} | {COIN}/USDC | Hyperliquid"` (как у HL: "33,152 | HYPE/USDC | Hyperliquid")
- Portfolio: `"Hyperliquid"`
- Earn: `"Hyperliquid"`

### 5. Favicon — если возможно, поменять на тёмный с бирюзовыми кружками

---

## PHASE 3.11 — BUILD + DEPLOY + VERIFY

1. `npm run build` — убедиться что нет ошибок TypeScript
2. Проверить все страницы локально:
   - /trade — полный функционал, 3 колонки, всё работает
   - /portfolio — данные загружаются, карточки и табы на месте
   - /earn — faucet работает, supply table показывается
3. Проверить disabled навигацию (Vaults/Staking/etc НЕ кликабельны)
4. Проверить мобильный layout (stacked, bottom nav)
5. `git add . && git commit && git push`
6. Vercel auto-deploy
7. Финальная проверка на https://hl-simulator.vercel.app

---

## ПОРЯДОК ВЫПОЛНЕНИЯ (СТРОГО!)

```
1. BUGFIX useAuth.ts (5 мин) → проверить что /portfolio и /faucet грузятся
2. Navigation.tsx переделка (15 мин)
3. Footer.tsx создать (5 мин)
4. trade/page.tsx — 3-колоночный grid (15 мин)
5. CoinInfoBar.tsx переделка (10 мин)
6. OrderForm.tsx переделка (15 мин)
7. OrderBook.tsx → средняя колонка + precision/unit selectors (10 мин)
8. AccountEquity.tsx создать (10 мин)
9. BottomTabsPanel.tsx расширить (10 мин)
10. portfolio/page.tsx полная переделка (20 мин)
11. earn/page.tsx создать (15 мин)
12. Мелочи: title, favicon, шрифты (5 мин)
13. Build + Deploy + Verify (5 мин)
```

## ВАЖНО:
- После КАЖДОЙ фазы — `npm run build` чтобы убедиться что нет ошибок
- НЕ ломать существующий функционал trading (placeOrder, closePosition, liquidation)
- Все "декоративные" элементы (disabled кнопки, заглушки) — ОБЯЗАТЕЛЬНЫ для визуального соответствия
- Если реальный Hyperliquid в Chrome доступен — СВЕРЯЙ каждый элемент визуально через скриншоты
- Приоритет: визуальное соответствие > новый функционал
