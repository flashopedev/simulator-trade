# HL Simulator

Демо-торговля для отработки стратегий на Hyperliquid.

![Trade Page](https://via.placeholder.com/800x400?text=Trade+Page)

## Возможности

- 📊 **Trade Page** — реальные графики и ордербук от Hyperliquid
- 💼 **Portfolio** — отслеживание позиций и истории
- 💰 **Faucet** — запрос демо USDC без ограничений
- 📱 **Mobile-first** — работает на iPhone и MacBook

## Технологии

- **Next.js 14** — React фреймворк
- **Supabase** — база данных + авторизация
- **Tailwind CSS** — стилизация
- **Hyperliquid API** — реальные данные рынка

---

## 🚀 Быстрый старт

### 1. Создать Supabase проект

1. Зайди на [supabase.com](https://supabase.com)
2. Создай новый проект (бесплатно)
3. Скопируй `Project URL` и `anon/public` ключ из Settings → API

### 2. Настроить базу данных

1. Открой SQL Editor в Supabase Dashboard
2. Скопируй и выполни содержимое `supabase/schema.sql`

### 3. Включить Email Auth

1. В Supabase: Authentication → Providers
2. Включи **Email** провайдер
3. (Опционально) Отключи email confirmation для теста

### 4. Настроить переменные окружения

```bash
cp .env.example .env.local
```

Заполни значения:
```
NEXT_PUBLIC_SUPABASE_URL=https://xxx.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=eyJhbGc...
```

### 5. Установить и запустить

```bash
npm install
npm run dev
```

Открой http://localhost:3000

---

## 📦 Деплой на Vercel

### Через CLI

```bash
npm install -g vercel
vercel
```

### Через GitHub

1. Пуш проект на GitHub
2. Зайди на [vercel.com](https://vercel.com)
3. Import → выбери репозиторий
4. Добавь Environment Variables:
   - `NEXT_PUBLIC_SUPABASE_URL`
   - `NEXT_PUBLIC_SUPABASE_ANON_KEY`
5. Deploy!

### Настроить Redirect URL

После деплоя добавь URL в Supabase:
1. Authentication → URL Configuration
2. Добавь `https://your-app.vercel.app` в Redirect URLs

---

## 📱 Структура проекта

```
hl-simulator/
├── src/
│   ├── app/
│   │   ├── trade/page.tsx      # Торговля
│   │   ├── portfolio/page.tsx  # Портфолио
│   │   ├── faucet/page.tsx     # Запрос демо средств
│   │   └── auth/callback/      # OAuth callback
│   ├── components/
│   │   ├── Chart.tsx           # График свечей
│   │   ├── OrderBook.tsx       # Ордербук
│   │   ├── OrderForm.tsx       # Форма ордера
│   │   ├── Positions.tsx       # Позиции и история
│   │   └── Navigation.tsx      # Навигация
│   ├── hooks/
│   │   ├── useAuth.ts          # Авторизация
│   │   ├── useTrading.ts       # Торговая логика
│   │   └── useMarketData.ts    # WebSocket данные
│   └── lib/
│       ├── supabase/           # Supabase клиенты
│       ├── hyperliquid.ts      # API Hyperliquid
│       └── utils.ts            # Утилиты
└── supabase/
    └── schema.sql              # SQL схема
```

---

## 🔧 Команды

```bash
npm run dev      # Запуск в режиме разработки
npm run build    # Сборка для продакшена
npm run start    # Запуск продакшен сборки
npm run lint     # Проверка кода
```

---

## 📝 Заметки

- **Данные рынка** — реальные с Hyperliquid API + WebSocket
- **Торговля** — симуляция, позиции хранятся в Supabase
- **Ликвидации** — автоматически проверяются каждые 5 секунд
- **Мобильная версия** — оптимизирована для iPhone

---

## 🛡️ Безопасность

- Row Level Security (RLS) включен на всех таблицах
- Каждый пользователь видит только свои данные
- Демо средства не имеют реальной ценности

---

Made with ❤️ for trading practice
